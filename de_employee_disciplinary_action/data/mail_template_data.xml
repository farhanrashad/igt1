<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->
    <data noupdate="1">
        <!--Email template -->
        <record id="email_template_edi_disciplinary_case" model="mail.template">
            <field name="name">Disciplinay Case: Send by email</field>
            <field name="model_id" ref="de_employee_disciplinary_case.model_hr_employee_disciplinary_case"/>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) |safe}</field>
            <field name="partner_to">${object.employee_id.id}</field>
            <field name="subject">Disciplinary case (Ref ${object.name or 'n/a'})</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Dear ${object.employee_id.name},
        <br /><br />
        Here is your
        % if object.name:
            Disciplinary Case <strong>${object.name}</strong>
        % else:
            Disciplinary Case
        <br /><br />
        Do not hesitate to contact us if you have any questions.
    </p>
</div>
            </field>
            <field name="report_template" ref="employee_disciplinary_case"/>
            <field name="report_name">Case_${(object.name or '').replace('/','_')}</field>
<!--             <field name="lang">${object.employee_id.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/> -->
        </record>
    </data>
</odoo>
